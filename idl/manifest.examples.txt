# Example/test-only IDLs (not part of the public API surface).
# Transport v2 is intentionally not represented in this FIDL manifest; its
# examples must consume the frozen Transport v2 codecs and fixtures directly.
flowersec/demo/v1/demo.fidl.json
