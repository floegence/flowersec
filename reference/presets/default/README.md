# default preset

First-party reference preset for the baseline Flowersec proxy/gateway limits.

This directory is a reference distribution path, not the stable contract itself.
The stable contract is `ProxyPresetManifest` plus the gateway `proxy.preset_file` consumer behavior.
