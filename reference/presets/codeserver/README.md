# codeserver preset

Deprecated first-party compatibility preset for older large-frame code-server style deployments.

It remains available for migration in v0.18.x, but named profiles are no longer part of the stable core surface.
