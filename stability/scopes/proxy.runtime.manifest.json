{
  "version": 1,
  "scope": "proxy.runtime",
  "scope_version": 1,
  "carrier": "ConnectArtifact.scoped[]",
  "payload_kind": "json_object",
  "critical_default": false,
  "consumer": "proxy_helpers",
  "resolver_contract": "exact_scope_and_version",
  "notes": [
    "This manifest freezes the proxy helper contract for proxy.runtime@1.",
    "Proxy helper entrypoints consume only scope_version=1 and fail fast on invalid payloads.",
    "Generic scope resolver APIs are governed by the shared public API contract."
  ]
}
