{
  "version": 1,
  "scope": "proxy.runtime",
  "scope_version": 1,
  "stability": "experimental",
  "carrier": "ConnectArtifact.scoped[]",
  "payload_kind": "json_object",
  "critical_default": false,
  "resolver_contract": "experimental",
  "notes": [
    "This manifest is an experimental source of truth for scoped metadata validation only.",
    "The carrier is stable in v0.18.x, but resolver registration and concrete payload semantics remain experimental."
  ]
}
