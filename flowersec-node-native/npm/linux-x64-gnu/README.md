# Flowersec Native Transport for Linux x64 glibc

This package contains the Linux x64 glibc prebuilt used by
`@floegence/flowersec-node-native`. Applications should use
`@floegence/flowersec-core/node` rather than importing this package directly.
